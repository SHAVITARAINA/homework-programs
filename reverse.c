#include <stdio.h>

int main() {
    int num = 10; // Starting point (you can change this value)
    
    while (num >= 1) {
        printf("%d\n", num);
        num--;
    }
    
    return 0;
}